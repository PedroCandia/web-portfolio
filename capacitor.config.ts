import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'portafolio-web',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
